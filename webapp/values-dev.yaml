replicaCount: 1

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

deployment:
  enabled: true

service:
  enabled: true
  type: ClusterIP
  port: 80

config:
  enabled: true
  indexHtml: |
    <h1>DEV ENVIRONMENT</h1>

ingress:
  enabled: false

autoscaling:
  enabled: false
