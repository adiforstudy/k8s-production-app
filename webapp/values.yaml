replicaCount: 5

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

serviceAccount:
  create: false
  name: ""

ingress:
  enabled: false

httpRoute:
  enabled: false

autoscaling:
  enabled: false

resources: {}

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 50

config:
  indexHtml: |
    <html>
    <body style="font-family: Arial;">
      <h1>Served via Helm ConfigMap</h1>
      <p>Helm is working 🚀</p>
    </body>
    </html>
